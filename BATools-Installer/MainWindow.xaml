<Window x:Class="BATools_Installer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BATools_Installer"
        mc:Ignorable="d"
        Title="BATools Installer" Height="540" Width="860"
        WindowStartupLocation="CenterScreen" Background="Black" Foreground="Black">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.Row="0">
            <TextBlock FontSize="22" FontWeight="SemiBold" Text="BATools Installer" Foreground="White" />
            <TextBlock Margin="0,6,0,0" Opacity="0.75"
                       Text="Install / Update / Uninstall for Revit 2026 (per-user)" Foreground="White"/>
        </StackPanel>

        <Border Grid.Row="1" Margin="0,14,0,14" Padding="12" BorderThickness="1" CornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="GitHub Release Source" FontWeight="SemiBold" Foreground="White"/>
                    <TextBlock Margin="0,4,0,0" Text="{Binding GitHubInfo}" Opacity="0.75"/>
                    <TextBlock Margin="0,8,0,0" Text="{Binding InstallInfo}" Opacity="0.75"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
                    <Button Width="130" Height="34" Margin="0,0,8,0" Content="Install" Click="Install_Click" Background="Black" Foreground="#FFFFEBEB" BorderBrush="Snow"/>
                    <Button Width="130" Height="34" Margin="0,0,8,0" Content="Update" Click="Update_Click" Background="#FF0A0A0A" BorderBrush="#FFFFF7F7" Foreground="#FFFFFBFB"/>
                    <Button Width="130" Height="34" Content="Uninstall" Click="Uninstall_Click" Background="#FF0A0A0A" BorderBrush="White" Foreground="#FFFFFCFC"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ProgressBar Grid.Row="0" Height="10" IsIndeterminate="{Binding IsBusy}" />

            <TextBox Grid.Row="1" Margin="0,12,0,0"
                     Text="{Binding LogText}" IsReadOnly="True"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas" FontSize="12" Background="White"/>
        </Grid>
    </Grid>
</Window>