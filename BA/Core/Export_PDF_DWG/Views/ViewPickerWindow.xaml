<Window x:Class="BA.Views.Export.ViewPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Style="{DynamicResource BaWindow}"
        Title="Pick Views"
        Width="600" Height="640"
        MinWidth="480" MinHeight="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/BA;component/UI/Resources/AppResources.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="ViewPickerHeaderStyle" TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="#16161A"/>
                <Setter Property="Foreground" Value="#F2F2F2"/>
                <Setter Property="BorderBrush" Value="#2C3440"/>
                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                <Setter Property="Padding" Value="10,6"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
            </Style>

            <Style x:Key="ViewPickerRowStyle" TargetType="DataGridRow">
                <Setter Property="Foreground" Value="#F2F2F2"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#1E2A3A"/>
                    </Trigger>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="#2A5A9E"/>
                        <Setter Property="Foreground" Value="#FFFFFF"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="ViewPickerCellStyle" TargetType="DataGridCell">
                <Setter Property="Foreground" Value="#F2F2F2"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="10,4"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Bottom"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="OK" Width="100" Margin="0,0,8,0"
                    IsDefault="True"
                    Style="{DynamicResource BaPrimaryButton}"
                    Click="Ok_Click"/>
            <Button Content="Cancel" Width="100"
                    IsCancel="True"
                    Style="{DynamicResource BaSecondaryButton}"/>
        </StackPanel>

        <Grid DockPanel.Dock="Top" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchBox" Grid.Column="0"
                     Style="{DynamicResource BaDarkTextBox}"
                     TextChanged="SearchBox_TextChanged"
                     ToolTip="Filters the list below by view name or view type as you type." />
            <Button Grid.Column="2" Content="Select All" Style="{DynamicResource BaSecondaryButton}"
                    Click="SelectAll_Click" />
            <Button Grid.Column="4" Content="Select None" Style="{DynamicResource BaSecondaryButton}"
                    Click="SelectNone_Click" />
        </Grid>

        <DataGrid x:Name="ViewsGrid"
                  Style="{DynamicResource BaDataGrid}"
                  ColumnHeaderStyle="{StaticResource ViewPickerHeaderStyle}"
                  RowStyle="{StaticResource ViewPickerRowStyle}"
                  CellStyle="{StaticResource ViewPickerCellStyle}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="False"
                  ColumnWidth="*">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Select" Binding="{Binding IsSelected}" Width="70"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" IsReadOnly="True" Width="3*"/>
                <DataGridTextColumn Header="Type" Binding="{Binding ViewType}" IsReadOnly="True" Width="1.5*"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>