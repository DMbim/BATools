<Window x:Class="BA.UI.LoadedFamilyBrowser.TypeParameterEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Type Parameters"
        Style="{DynamicResource BaWindow}"
        Width="520"
        Height="600"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/BA;component/UI/Resources/AppResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding TypeName, StringFormat='Parameters: {0}'}"
                   FontWeight="Bold"
                   Margin="0,0,0,8" />

        <Border Grid.Row="1"
                Style="{StaticResource BaDarkPanelBorder}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Parameters}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="160" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Text="{Binding Name}"
                                           VerticalAlignment="Center"
                                           TextTrimming="CharacterEllipsis" />
                                <TextBox Grid.Column="1"
                                         Text="{Binding EditedValueText, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource BaDarkTextBox}" />
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <TextBlock Grid.Row="2"
                   Text="{Binding StatusMessage}"
                   Foreground="Orange"
                   Margin="0,8,0,0"
                   TextWrapping="Wrap" />

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="Cancel"
                    Style="{StaticResource BaSecondaryButton}"
                    Width="90"
                    Margin="0,0,8,0"
                    Command="{Binding CancelCommand}" />
            <Button Content="Save"
                    Style="{StaticResource BaPrimaryButton}"
                    Width="90"
                    Command="{Binding SaveCommand}" />
        </StackPanel>
    </Grid>
</Window>