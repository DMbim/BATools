<Window x:Class="BA.UI.LoadedFamilyBrowser.LoadedFamilyBrowserSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Loaded Family Browser Settings"
        Style="{DynamicResource BaWindow}"
        Width="380"
        Height="480"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/BA;component/UI/Resources/AppResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Categories included in the loaded family tree:"
                   Margin="0,0,0,8" />

        <Border Grid.Row="1"
                Style="{StaticResource BaDarkPanelBorder}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="CategoryItemsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Name}"
                                      IsChecked="{Binding IsEnabled, Mode=TwoWay}"
                                      Style="{StaticResource BaDarkPanelCheckBox}"
                                      Margin="4" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="Cancel"
                    Style="{StaticResource BaSecondaryButton}"
                    Width="90"
                    Margin="0,0,8,0"
                    Click="CancelButton_Click" />
            <Button Content="Save"
                    Style="{StaticResource BaPrimaryButton}"
                    Width="90"
                    Click="SaveButton_Click"
                    IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>