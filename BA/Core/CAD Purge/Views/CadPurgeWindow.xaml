<!-- File: BA_Tools/CadPurge/Views/CadPurgeWindow.xaml -->
<Window x:Class="BA.CadPurge.Views.CadPurgeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BA CAD Purge"
        Width="980" Height="640"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>
    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Scan" Command="{Binding ScanCommand}" Width="90" Margin="0,0,6,0"/>
            <Button Content="Select All Mappable" Command="{Binding SelectAllMappableCommand}" Width="140" Margin="0,0,6,0"/>
            <Button Content="Clear Selection" Command="{Binding ClearSelectionCommand}" Width="120" Margin="0,0,6,0"/>
            <Button Content="Apply Selected" Command="{Binding ApplySelectedCommand}" Width="120" Margin="0,0,20,0"/>
            <TextBlock Text="Working..." Foreground="DarkOrange" VerticalAlignment="Center"
                       Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"/>
        </StackPanel>

        <TextBlock DockPanel.Dock="Bottom" Text="{Binding StatusMessage}" Margin="0,10,0,0" TextWrapping="Wrap"/>

        <TabControl>
            <TabItem Header="Line Patterns">
                <DataGrid ItemsSource="{Binding LinePatternCandidates}" AutoGenerateColumns="False"
                          CanUserAddRows="False" CanUserDeleteRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="220" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Usage" Binding="{Binding UsageCount}" Width="70" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Proposed Target" Binding="{Binding ProposedTargetName}" Width="180" IsReadOnly="True"/>
                        <DataGridTemplateColumn Header="Action" Width="170">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox ItemsSource="{Binding AvailableActions}"
                                              SelectedItem="{Binding SelectedAction, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Status" Binding="{Binding StatusDisplay}" Width="*" IsReadOnly="True"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="Text Styles">
                <DataGrid ItemsSource="{Binding TextStyleCandidates}" AutoGenerateColumns="False"
                          CanUserAddRows="False" CanUserDeleteRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="220" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Usage" Binding="{Binding UsageCount}" Width="70" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Proposed Target" Binding="{Binding ProposedTargetName}" Width="180" IsReadOnly="True"/>
                        <DataGridTemplateColumn Header="Action" Width="170">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox ItemsSource="{Binding AvailableActions}"
                                              SelectedItem="{Binding SelectedAction, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Status" Binding="{Binding StatusDisplay}" Width="*" IsReadOnly="True"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="DWG Imports (Report Only)">
                <DataGrid ItemsSource="{Binding DwgImportReport}" AutoGenerateColumns="False"
                          CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200"/>
                        <DataGridTextColumn Header="Category" Binding="{Binding CategoryName}" Width="160"/>
                        <DataGridCheckBoxColumn Header="Linked" Binding="{Binding IsLinked}" Width="60"/>
                        <DataGridTextColumn Header="Workset" Binding="{Binding WorksetName}" Width="120"/>
                        <DataGridTextColumn Header="Owner View" Binding="{Binding OwnerViewName}" Width="160"/>
                        <DataGridTextColumn Header="Linked File Path" Binding="{Binding LinkedFilePath}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>