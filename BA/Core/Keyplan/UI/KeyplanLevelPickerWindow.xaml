<Window x:Class="BA.UI.KeyplanGrid.KeyplanLevelPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Keyplan Grid - Select Level"
        Height="460" Width="460"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#FF1E1E1E"
        Foreground="#FFE0E0E0"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#FF3A3A3A"/>
            <Setter Property="Foreground" Value="#FFE0E0E0"/>
            <Setter Property="BorderBrush" Value="#FF555555"/>
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="MinWidth" Value="90"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF505050"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="#FF707070"/>
                    <Setter Property="Background" Value="#FF2A2A2A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFE0E0E0"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="#FF707070"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#FF3D5A80"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsMouseOver" Value="True"/>
                        <Condition Property="IsEnabled" Value="True"/>
                        <Condition Property="IsSelected" Value="False"/>
                    </MultiTrigger.Conditions>
                    <Setter Property="Background" Value="#FF333333"/>
                </MultiTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Select the level to generate the keyplan for:"
                   FontSize="14"
                   Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1"
                   Text="Levels marked 'not set up' need a KP_GrossArea(KeyPlan) area plan with a boundary. Select one for instructions and setup options."
                   FontSize="11"
                   Foreground="#FFA0A0A0"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"/>

        <ListBox x:Name="LevelListBox"
                 Grid.Row="2"
                 Background="#FF2A2A2A"
                 BorderBrush="#FF444444"
                 BorderThickness="1"
                 SelectionMode="Single"
                 DisplayMemberPath="DisplayName"
                 MouseDoubleClick="LevelListBox_MouseDoubleClick"
                 SelectionChanged="LevelListBox_SelectionChanged"/>

        <TextBlock x:Name="InstructionsText"
                   Grid.Row="3"
                   Margin="0,10,0,0"
                   FontSize="11"
                   Foreground="#FFC0A060"
                   TextWrapping="Wrap"
                   Visibility="Collapsed"/>

        <StackPanel Grid.Row="4"
            Orientation="Horizontal"
            HorizontalAlignment="Left"
            Margin="0,10,0,0">
            <Button x:Name="BtnCreateView"
            Content="Create Area Plan View"
            Click="BtnCreateView_Click"
            Visibility="Collapsed"
            Margin="0,0,8,0"/>
            <Button x:Name="BtnOpenView"
            Content="Open Area Plan View"
            Click="BtnOpenView_Click"
            Visibility="Collapsed"
            Margin="0,0,8,0"/>
            <Button x:Name="BtnRefresh"
            Content="↻ Refresh"
            Click="BtnRefresh_Click"
            Visibility="Collapsed"/>
        </StackPanel>
        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,14,0,0">
            <Button x:Name="BtnOk"
                    Content="OK"
                    IsDefault="True"
                    Click="BtnOk_Click"
                    IsEnabled="False"/>
            <Button x:Name="BtnCancel"
                    Content="Cancel"
                    IsCancel="True"
                    Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>